---
# Copy to group_vars/all/vault.yml, replace the values, then encrypt that file
# with ansible-vault before committing it.
vault_postgres_password: "replace-with-a-long-random-password"
vault_cockpit_admin_password_hash: "$6$REPLACE_WITH_SHA512_PASSWORD_HASH"
